<%= link_to( @user.try(:name), :controller => 'stuff_to_do', :user_id => @user.id, :only_path => false) %> has only <%= @count %> recommended items left in their <%= link_to "Stuff to Do", :controller => 'stuff_to_do', :user_id => @user.id, :only_path => false %> which is below the threshold of <%= @threshold %>.
